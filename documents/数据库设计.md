#### user

复用博客系统的登录接口，单点登录（多用户/多端同步历史需要）

> 博客系统的具体表结构（博客系统用的mysql）如下，本系统不再需要user表
>
> ==注意保证两个系统jwt库版本一致==

| 字段名          | 数据类型      | 说明         | 备注                                    |      |
| --------------- | ------------- | ------------ | --------------------------------------- | ---- |
| id              | bigint        | 主键         | 自增                                    |      |
| username        | varchar(32)   | 用户名       | 唯一                                    |      |
| password        | varchar(255)  | 密码         | 不要存明文密码! 存哈希值                |      |
| phone           | varchar(11)   | 手机号       |                                         |      |
| email           | varchar(100)  |              | 邮箱，可用于订阅文章，必须唯一          |      |
| avatar          | varchar(1000) | 头像图片路径 |                                         | 可选 |
| gender          | tinyint       | 性别         | 约定：0保密/未知 1男 2女                | 可选 |
| status          | tinyint       | 账号状态     | 0正常  1锁定                            | 必须 |
| last_login_time | datetime      | 最后登录时间 | 记录最后登录时间，用于安全审计          |      |
| role            | tinyint       | 角色         | 约定：0管理员  1作者 2用户（默认设为2） | 必须 |
| created_at      | datetime      | 创建时间     |                                         |      |
| updated_at      | datetime      | 最后修改时间 |                                         |      |



---

#### group



| 字段名     | 数据类型     | 说明     | 备注                                    |
| ---------- | ------------ | -------- | --------------------------------------- |
| id         | bigserial    | 分组ID   | [非空]**主键**；                        |
| user_id    | bigserial    | 所属用户 | [非空]外键 → `user.id`；不可为空        |
| name       | varchar(50)  | 分组标题 | [非空]必填；如 “对话 123456”、“xxx.pdf” |
| sort       | int4         | 用于排序 | [非空]默认为0，降序排序                 |
| status     | boolean      | 状态     | [非空]true正常 false已删除（软删）      |
| created_at | timestamp(0) | 创建时间 | [非空]默认 now()（用于历史列表排序）    |





---

#### chat_session

会话表：统一承载前端的 **AI聊天(chat)** / **Astra** / **哄哄模拟器(game)** / **智能客服(service)** 四类会话。

> 和 `user` 是多对一关系（不允许 `user_id` 为空，登录之后才能使用）。

| 字段名          | 数据类型      | 说明             | 备注 |
| --------------- | ------------- | ---------------- | ---- |
| id              | bigserial | 会话ID           | [非空]**主键**； |
| user_id         | bigserial  | 所属用户         | [非空]外键 → `user.id`；不可为空 |
| group_id | bigserial | 分组id | 外键 → `group.id`；可为空 |
| type | varchar(16) | 会话类型 | **约定：`chat / astra / service`** |
| title           | varchar(255)  | 会话标题         | [非空] 如 “对话 123456”、“xxx.pdf” |
| status          | boolean       | 状态             | [非空]true正常 false已删除（软删） |
| is_top | boolean | 是否置顶 | [非空]true置顶 false不置顶 |
| last_active_at | timestamp(0)  | 最后活跃时间     | [非空]用于历史列表排序 |
| created_at | timestamp(0)  | 创建时间         | [非空]默认 now() |
| updated_at | timestamp(0)  | 更新时间         | [非空]默认 now() |

> 需要type字段，查询会话列表的时候需要这个字段来区分 是查询多模态对话系统/知识库的会话列表

> 索引建议：`(user_id, type, last_message_at desc)`、`(type, last_message_at desc)`。



---

#### chat_message(chat_history)

消息表：存储每个会话中的消息（user/assistant/system），支持流式输出场景下的“最终落库”。

> 和 `chat_session` 是多对一关系。

| 字段名        | 数据类型      | 说明             | 备注 |
| ------------ | ------------- | ---------------- | ---- |
| id           | bigserial     | 主键             | [非空]自增 |
| session_id   | bigserial | 会话ID           | [非空]外键 → `chat_session.id` |
| role         | varchar(16)   | 角色             | [非空]默认user，约定：`user / assistant / system` |
| content      | text          | 消息内容         | [非空]纯文本或 Markdown（客服/Astra 的 assistant 往往是 Markdown） |
| status | boolean | 状态 | [非空]true 正常；false 已删除 |
| metadata_json| jsonb         | 扩展信息         | 可存：模型名、token统计、thinking片段汇总、解析到的“原谅值”等 |
| created_at  | timestamp(0)  | 创建时间         | [非空]默认 now() |

> 索引建议：`(session_id, id)`（按消息顺序分页）、`(session_id, last_active_at)`。
>
> 可加`user_id, status, last_active_at`联合索引



#### model

模型信息表

| 字段名         | 数据类型     | 说明                 | 备注                          |
| -------------- | ------------ | -------------------- | ----------------------------- |
| id             | bigserial    | 主键                 | [非空]自增                    |
| name           | varchar(32)  | 模型名称[唯一标识符] | [非空]                        |
| description    | varchar(50)  | 模型描述             |                               |
| is_recommended | boolean      | 是否推荐             | [非空]是否推荐                |
| is_beta        | boolean      | 是否为Beta版本       | [非空]                        |
| sort           | int4         | 排序字段             | [非空]降序排序，默认为0       |
| status         | boolean      | 状态                 | [非空]true 正常；false 已删除 |
| created_at     | timestamp(0) | 创建时间             | [非空]默认 now()              |
| updated_at     | timestamp(0) | 更新时间             | [非空]默认 now()              |





---

#### attachment

> #### (multimodal_data)

多模态数据支持表：对应前端 AIChat 上传的 image/audio/video 文件（以及未来可能的任意文件）。

> 可以绑定到“某条消息”

| 字段名        | 数据类型      | 说明             | 备注 |
| ------------ | ------------- | ---------------- | ---- |
| id           | bigserial     | 主键             | [非空]自增不可为空 |
| message_id   | bigserial | 消息ID           | [非空]外键 → `chat_message.id`； |
| file_name    | varchar(512)  | 原始文件名       | [非空] |
| mime_type    | varchar(128)  | MIME 类型        | [非空]如 `image/png`、`audio/mpeg`、`video/mp4` |
| source_type | varchar(128) | 来源类型 | 来源类型: wanx_flux, upload, etc. |
| file_size | bigserial | 文件大小 | 最大约9EB，覆盖所有可能文件大小 |
| ~~storage_type~~ | varchar(16)   | 存储类型         | [非空]约定：` oss / others `（按部署方式选择）<br />本项目不需要该字段 |
| storage_path | varchar(2048) | 存储路径/URL     | [非空]本地路径或对象存储URL/Key |
| sha256       | varchar(64)   | 内容哈希         | [非空]用于去重/完整性校验 |
| created_at  | timestamp(0)  | 创建时间         | [非空]默认 now() |

设置上传大小限制，防止用户上传过大的文件导致服务器资源耗尽

> 索引建议：`(session_id)`、`(message_id)`、`(sha256)`。
>
> 
>
> ==sha256的作用==
>
> - 上传相同文件时，可以通过sha256检查是否已存在，避免重复存储，节省存储空间
> - 可以用于**构建唯一文件标识**，如`/files/{sha256}.{ext}`，避免文件名冲突
>
> ==mime_type字段==
>
> * 安全防护
>
>   - "在文件上传场景中，服务器可以通过校验MIME类型限制用户上传的文件类型，防止恶意文件（如可执行文件）被上传。"
>   - 示例（Python）：`allowed_mime = {'image/jpeg', 'image/png'} if uploaded_file.mimetype not in allowed_mime: raise ValueError("仅支持JPEG或PNG图片！")`
>
> * 正确文件处理
>
>   - 知识库[6]提到："浏览器根据Content-Type响应头中的MIME类型决定如何处理文件"
>   - 例如：`image/jpeg`会显示为图片，`application/pdf`会触发PDF下载
>
> * 文件类型验证
>
>   - 上传文件时，系统可以验证文件的实际类型是否与上传的扩展名一致
>   - 防止用户上传"恶意文件"（如将.exe文件命名为.jpg）
>
> * 用户体验
>
>   - 系统可以正确识别文件类型，提供相应的预览功能
>   - 例如：图片文件可以显示缩略图，PDF文件可以提供预览功能
>
>   **示例**：如果用户上传了一个文件，名为`document.jpg`，但实际内容是PDF，系统可以通过`mime_type`字段检测到这不是图片，而是PDF，从而拒绝上传或提示用户。



---

### 后期

####  **用户行为分析表（重要补充）**

- 用于分析用户使用习惯，优化AI服务
- "用户画像"需要基础数据

```
| 字段名 | 数据类型 | 说明 | 备注 |
| --- | --- | --- | --- |
| id | BIGINT AUTO_INCREMENT | 行为ID | 主键 |
| user_id | BIGINT | 用户ID | 外键 → user.id |
| action_type | VARCHAR(32) | 行为类型 | 如"chat_start", "pdf_upload" |
| action_data | JSON | 行为数据 | |
| create_time | TIMESTAMP | 行为时间 | 默认 NOW() |
```



> #### **模型使用统计表（重要补充）**
>
> 
>
> - 用于监控AI模型使用情况，优化资源分配
> - 企业级应用需要监控模型负载
>
> **建议表结构**：
>
> ```
> | 字段名 | 数据类型 | 说明 | 备注 |
> | --- | --- | --- | --- |
> | id | BIGINT AUTO_INCREMENT | 统计ID | 主键 |
> | model_name | VARCHAR(64) | 模型名称 | 如"qwen-max" |
> | session_count | INT | 会话数量 | |
> | token_count | BIGINT | Token使用量 | |
> | cost | DECIMAL(10,2) | 费用 | |
> | create_date | DATE | 计日期 | |
> ```



### Astra相关

> kb：指knowledge base



#### kb_library

`kb_library` 知识库表：存储用户创建的知识库基本信息。

> 和 `user` 是多对一关系（`owner_id` 指向用户）。

| 字段名      | 数据类型     | 说明         | 备注             |
| ----------- | ------------ | ------------ | ---------------- |
| id          | bigserial    | 主键         | [非空]自增       |
| name        | varchar(32)  | 知识库名称   | [非空]           |
| description | varchar(255) | 知识库描述   |                  |
| type        | varchar(16)  | 知识库类型   | [非空]约定：`personal / team` |
| owner_id    | bigserial    | 所属用户     | [非空]外键 → `user.id` |
| is_top      | boolean      | 是否置顶     | [非空]默认false  |
| cover_image | varchar(600) | 封面图片地址 |                  |
| created_at  | timestamp(0) | 创建时间     | [非空]默认 now() |
| updated_at  | timestamp(0) | 更新时间     | [非空]默认 now() |

> 不采用软删除，硬删除时一并清理知识列表、Media、分片和OSS文件









#### kb_media

`kb_media` 媒体表：存储知识库内上传的文件元信息，关联原始文件与解析任务。

> 和 `kb_library` 是多对一关系；状态流转驱动解析消费者工作。

| 字段名        | 数据类型      | 说明           | 备注 |
| ------------- | ------------- | -------------- | ---- |
| id            | bigserial     | 主键           | [非空]自增 |
| library_id    | bigserial     | 所属知识库     | [非空]外键 → `kb_library.id` |
| file_name     | varchar(512)  | 原始文件名     | [非空] |
| mime_type     | varchar(128)  | MIME 类型      | [非空]如 `application/pdf`、`image/png` |
| file_size     | bigserial     | 文件大小       | 单位：字节 |
| storage_path  | varchar(2048) | 存储路径/OSS Key | [非空]OSS文件路径 |
| sha256        | varchar(64)   | 内容哈希       | [非空]用于去重/完整性校验 |
| status        | varchar(16)   | 解析状态       | [非空]约定：`PENDING / PARSING / PARSED / FAILED` |
| total_chunks  | int4          | 总分片数       | 解析完成后填充 |
| parsed_chunks | int4          | 已解析分片数   | 实时更新，用于进度展示 |
| error_message | varchar(512)  | 错误信息       | 解析失败时记录原因 |
| created_at    | timestamp(0)  | 创建时间       | [非空]默认 now() |
| updated_at    | timestamp(0)  | 更新时间       | [非空]默认 now() |

> Media 状态流转：`PENDING（入队）→ PARSING（被消费者取出）→ PARSED（成功）| FAILED（超过重试上限）`
>
> 消费者处理开始时更新 `status = PARSING`，处理完成后更新为 `PARSED` 或 `FAILED`，确保前端轮询时能看到最新进度。







#### kb_chunk

`kb_chunk` 分片表：存储解析后的文本分片及向量嵌入，用于 RAG 检索。

> 向量维度 1024（text-embedding-v3），使用 pgvector 存储和检索。

| 字段名      | 数据类型      | 说明           | 备注 |
| ----------- | ------------- | -------------- | ---- |
| id          | bigserial     | 主键           | [非空]自增 |
| library_id  | bigserial     | 所属知识库     | [非空]外键 → `kb_library.id` |
| media_id    | bigserial     | 关联媒体文件   | [非空]外键 → `kb_media.id` |
| content     | text          | 原始文本内容   | [非空] |
| embedding   | vector(1024)  | 向量嵌入       | DashScope text-embedding-v3 生成 |
| chunk_index | int4          | 分片序号       | [非空]同一media内的顺序号 |
| metadata    | jsonb         | 扩展元信息     | 可存储：页码、来源文件、章节标题等 |
| created_at  | timestamp(0)  | 创建时间       | [非空]默认 now() |

> **索引建议**：
> - `(library_id)` — 按知识库检索
> - `(media_id)` — 按媒体文件检索
> - `USING hnsw (embedding vector_cosine_ops)` — HNSW 向量索引，加速相似度查询
>
> **相似度查询示例**：
> ```sql
> SELECT id, content, 1 - (embedding <=> $query_embedding) AS similarity
> FROM kb_chunk
> WHERE library_id = $library_id
> ORDER BY embedding <=> $query_embedding
> LIMIT 10;
> ```
>
> 需要在 PostgreSQL 中启用 pgvector 扩展：`CREATE EXTENSION IF NOT EXISTS vector;`



> **media_id 外键什么作用？**
>
> | 场景           | 为什么需要 media_id                                          |
> | -------------- | ------------------------------------------------------------ |
> | **溯源**       | 回答时标注"内容来自 xxx.pdf 第5页"，用户可跳转原文           |
> | **增量更新**   | `DELETE FROM kb_chunk WHERE media_id = ?` — 只删这一个文件的块 |
> | (**幂等保护**) | `media_id + chunk_index` 可建唯一约束，防止重复插入          |
>
> (**检索只关心"哪个知识库"**，`media_id` 对检索算法没有直接作用)

> #### 为什么需要`chunk_index`字段
>
> `chunk_index` 是**文档内分块的顺序号**，它的作用不在检索层面，而在**文档结构维护**层面：
>
> ##### 核心作用
>
> ##### 1. 保持原文阅读顺序
>
> 一个 50 页的 PDF 被切成 100 个 chunk，召回时需要按原始顺序组织，而不是按相似度排序：
>
> 
>
> ```
> 文档顺序：   chunk_0 → chunk_1 → chunk_2 → ... → chunk_99
>                 ↓
> RAG Prompt 引用时：按 chunk_index 排序拼接，保持上下文连贯性
> ```
>
> 否则 LLM 看到的是语义相似但逻辑跳跃的内容（如先讲"结论"再讲"背景"）。
>
> ##### 2. 支持确定性引用
>
> 在回答中标注来源时，需要精确到"第几块"：
>
> ```
> 回答：Spring Boot 的自动配置原理是...
> 来源：[技术文档-第3页] chunk_index=42
>      [技术文档-第5页] chunk_index=56
> ```
>
> 用户点击引用能跳转到原文位置。
>
> ##### 3. 支持增量更新（幂等保护）
>
> 重新解析同一文件时：
>
> ```sql
> -- 先删旧块
> DELETE FROM kb_chunk WHERE media_id = ? AND chunk_index >= 0;
> 
> -- 再插新块（chunk_index 保持连续）
> INSERT INTO kb_chunk (media_id, chunk_index, content, ...) VALUES (?, 0, ...);
> INSERT INTO kb_chunk (media_id, chunk_index, content, ...) VALUES (?, 1, ...);
> ```
>
> `chunk_index` + `media_id` 可以构成**唯一约束**，防止重复插入。
>
> ------
>
> ##### 但它不参与检索
>
> ```sql
> -- 向量检索只看 embedding，不看 chunk_index
> ORDER BY embedding <=> #{queryEmbedding}
> 
> -- BM25 检索也只看 content，不看 chunk_index
> ORDER BY ts_rank(to_tsvector('chinese', content), plainto_tsquery('chinese', ?))
> ```
>
> `chunk_index` 是**序号线**，不是**检索线**。
>
> ------
>
> ##### 如果没有会怎样？
>
> - 无法保持召回内容的原文顺序 → RAG 回答可能逻辑混乱
> - 无法精确标注"内容来自第几块" → 用户无法溯源
> - 增量更新时无法精确匹配旧块 → 必须全量删除再插入
> - 缺少唯一约束 → 重复解析会积累重复 chunk
>
> 所以 `chunk_index` 是**文档结构元数据**，服务于结果的**可读性和可追溯性**，而非检索算法本身。





> #### kb_tag（暂未支持）
>
> 知识库标签管理
>
> 暂时不加这个功能













