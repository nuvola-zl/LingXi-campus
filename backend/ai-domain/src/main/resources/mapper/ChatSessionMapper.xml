<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.hazenix.hazeaihub.domain.ai.mapper.ChatSessionMapper">


    <select id="list" resultType="top.lingxi.campus.domain.ai.entity.ChatSession">
        SELECT id, user_id, group_id, type, title, status,
               is_top, last_active_at, created_at, updated_at
        FROM chat_session WHERE user_id = #{userId}
        ORDER BY is_top DESC, created_at DESC
        LIMIT #{limit}
    </select>
</mapper>
