<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.hazenix.hazeaihub.domain.ai.mapper.ChatMessageMapper">

    <select id="selectBySessionIdOrderByCreatedAt" resultType="top.lingxi.campus.domain.ai.entity.ChatMessage">
        SELECT * FROM (
                          SELECT * FROM chat_message
                          WHERE session_id = #{sessionId} and status = true
                          ORDER BY created_at DESC
                              LIMIT #{limit}
                      ) AS recent_messages
        ORDER BY created_at ASC;
    </select>
</mapper>
